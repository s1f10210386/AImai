import styles from './index.module.css';

const Chat = () => {
  return (
    <div className={styles.container}>
      <div className={styles.roomName}>CHAT</div>
    </div>
  );
};

export default Chat;
